import { id } from "../utils";

export default async ({ map, assets }) => {

	const lines = map.split("\n");
	const nextId = (id => () => id("e"))(id(0));
	
	//-- Load entities into pseudo voxel structure
	//-- Compress voxel structure
	//-- Call load from the assets for each compressed structure..

	return {}
};



